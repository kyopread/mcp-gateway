# Copy to .env and set private values. Compose reads .env automatically.
# Gateway does not automatically load .env; inject variables or use node --env-file.
KEYCLOAK_ADMIN_PASSWORD=
GATEWAY_SMOKE_CLIENT_SECRET=
VENDOR_MCP_API_KEY=
INTERNAL_MCP_CLIENT_SECRET=
KEYCLOAK_DIRECTORY_SECRET=
KEYCLOAK_CONSOLE_PASSWORD=
